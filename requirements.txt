# Pin Gradio 5.50.x (theme/css no Blocks; launch NÃO aceita theme/css)
# Gradio 6 moveu theme/css para launch() — não usar no Render ainda.
gradio==5.50.0

# Graph
networkx>=3.2
plotly>=5.22.0
numpy>=1.26.0
pandas>=2.1.0

# Vault loading (GitHub clone)
GitPython>=3.1.43
